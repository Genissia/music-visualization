librosa>=0.11.0
numpy>=2.4.6
moderngl
PyGLM
Pillow
# cupy may require a specific version based on your CUDA version (e.g., cupy-cuda12x)
cupy
perlin-noise

# System dependencies:
# - ffmpeg (must be installed and in your PATH)
